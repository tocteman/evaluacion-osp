import React, { FunctionComponent } from "react"
import { PerfilProps } from "../types"

const Perfil: FunctionComponent<PerfilProps> = ({user}) => {
  return (
    <div></div>
  )
}

export default Perfil